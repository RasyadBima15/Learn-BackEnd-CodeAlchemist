//  + string, number, boolean (tipe data primitive)

const ajul = "Ajul";
let brand: string; //kita setting tipe data nya
brand = "Khong Guan";
brand = "Monde";

let weight: number = 50;
weight = 50.5;

let isOld: boolean;
isOld = true;
isOld = !isOld;

// brand = 3.14; // ini error karena tipe data yang digunakan adalah string
console.log(typeof ajul);
console.log(brand);
